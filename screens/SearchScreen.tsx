import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const SearchScreen = () => {
  return (
    <SafeAreaView>
      <Text>Search</Text>
    </SafeAreaView>
  );
};

export default SearchScreen;
