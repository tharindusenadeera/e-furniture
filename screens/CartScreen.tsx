import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const CartScreen = () => {
  return (
    <SafeAreaView>
      <Text>Cart</Text>
    </SafeAreaView>
  );
};

export default CartScreen;
